apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    app: notification-service
    tier: backend
spec:
  selector:
    app: notification-service
  ports:
    - port: 80
      targetPort: 5004
  type: ClusterIP
